[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.pyright]
include = ["src", "examples", "experiments"]
typeCheckingMode = ["basic"]
strict = ["src", "examples"]
